apply plugin: 'maven-publish'

def LIB_GROUP_ID = 'com.demo.kmmlibrary'
def LIB_ARTIFACT_ID = 'kmmlibrary'
def LIB_VERSION = '1.0.0'
def LIB_VERSION_SNAPSHOT = '1.0.0-SNAPSHOT'

android {
    publishing {
        singleVariant("debug") {
            withSourcesJar()
            withJavadocJar()
        }
        singleVariant("release") {

        }
    }
}


afterEvaluate {
    publishing {
        repositories {
            maven {
                name = "GitHubPackages"
                url = uri("https://maven.pkg.github.com/jaypatel-208/kmm_library")
                credentials {
                    username = "jaypatel-208"
                    password = "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"
                }
            }
        }

        publications {
            release(MavenPublication) {
                from components.release
                groupId LIB_GROUP_ID
                artifactId LIB_ARTIFACT_ID
                version LIB_VERSION
            }

            debug(MavenPublication) {
                from components.debug
                groupId LIB_GROUP_ID
                artifactId LIB_ARTIFACT_ID
                version LIB_VERSION_SNAPSHOT
            }
        }
    }
}